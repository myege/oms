<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.what21.dao.BuyerLimitDao">


	
	<insert id="insert" parameterType="com.what21.model.BuyerLimit" >
		   insert into t_buyer_limit (id, name, nameId, 
      totalUsed, createTime)
    values (#{id,jdbcType=INTEGER}, #{name,jdbcType=VARCHAR}, #{nameid,jdbcType=VARCHAR}, 
      #{totalused,jdbcType=DOUBLE}, #{createtime,jdbcType=TIMESTAMP})
  	</insert>

	<select id="findByNameAndNameId" resultType="com.what21.model.BuyerLimit">
		select * from t_buyer_limit where name = #{0} and nameId = #{1}	
	</select>
	
	<update id="update" parameterType="com.what21.model.BuyerLimit">
	    update t_buyer_limit
    set name = #{name,jdbcType=VARCHAR},
      nameId = #{nameid,jdbcType=VARCHAR},
      totalUsed = #{totalused,jdbcType=DOUBLE},
      createTime = #{createtime,jdbcType=TIMESTAMP}
    where id = #{id,jdbcType=INTEGER}
  </update>
  



  
</mapper>